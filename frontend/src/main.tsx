import React from 'react';
import ReactDOM from 'react-dom/client';
import { GeminiMortgage } from './pages/GeminiMortgage';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <GeminiMortgage />
    </React.StrictMode>
);
